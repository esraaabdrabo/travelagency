import 'package:flutter/material.dart';

class MobileVisaScreen extends StatelessWidget {
  const MobileVisaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
