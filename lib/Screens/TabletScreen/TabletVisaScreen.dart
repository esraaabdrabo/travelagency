import 'package:flutter/material.dart';

class TabletVisaScreen extends StatelessWidget {
  const TabletVisaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
